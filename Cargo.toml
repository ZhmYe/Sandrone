[package]
name = "codex-auto-dev-workflow"
version = "0.1.0"
edition = "2024"
description = "CLI-first workflow for turning work items into plans, isolated worktrees, change docs, and PR-ready branches."

[[bin]]
name = "codex-auto-dev"
path = "src/main.rs"
