# README Dashboard Command Docs Plan

## 目标与顺序

1. 补充安装章节，说明 `cad` 别名可替代 `codex-auto-dev`。
2. 补充快速开始，说明 dashboard 页面为空时如何刷新 registry。
3. 补充 Dashboard 命令参考，说明 host/port、json、registry 和旧 workspace 登记。
4. 补充 Dashboard 展示规则，说明三类标签和局部滚动。
5. 登记 proposal 并运行文档校验。

## 验证

- `python3 scripts/validate_proposals.py`
- `git diff --check`
