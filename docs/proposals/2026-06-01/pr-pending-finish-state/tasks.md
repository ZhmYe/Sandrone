# PR Pending Finish State Tasks

- [x] 新增 `wait-finish` 状态 rank，并让 tick 不再自动处理该状态。
- [x] 调整 `finish` 首次交付后的状态写入。
- [x] 新增 `pr-status` 命令和 PR 状态解析。
- [x] 支持 legacy `finished` 根据 PR 脚本结果回退为 `wait-finish`。
- [x] 更新 dashboard 对 `wait-finish` 的显示和统计。
- [x] 更新 README、skill 和 proposal 索引。
- [x] 更新测试并运行验证命令。
