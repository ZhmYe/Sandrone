# Tasks: Codex CLI Environment Docs

- [x] 在 README 中新增 Codex CLI 环境变量章节。
- [x] 记录 `SANDRONE_CODEX_BIN`、PATH 和 `SANDRONE_CODEX_APP` 的解析顺序。
- [x] 添加 `~/.zshrc` 推荐配置块。
- [x] 说明 `~/.zprofile`、bash、GUI/LaunchAgent 和 `launchctl setenv` 场景。
- [x] 说明代理变量继承方式。
- [x] 更新 proposal 索引和本 proposal 文档包。
- [x] 运行文档格式和 proposal 验证。
